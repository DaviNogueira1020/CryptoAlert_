# Supabase environment example for local development
# Copy to `.env` and fill with your project's values

# Option 1 (preferred): full Supabase URL + anon key
VITE_SUPABASE_URL=https://<your-project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<your-anon-public-key>

# Option 2: project id and anon key (alternate)
# VITE_SUPABASE_PROJECT_ID=<your-project-ref>
# VITE_SUPABASE_ANON_KEY=<your-anon-public-key>

# After creating `.env`, restart the dev server: `npm run dev`
